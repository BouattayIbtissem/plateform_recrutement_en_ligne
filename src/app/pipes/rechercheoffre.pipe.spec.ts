import { RechercheoffrePipe } from './rechercheoffre.pipe';

describe('RechercheoffrePipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheoffrePipe();
    expect(pipe).toBeTruthy();
  });
});
