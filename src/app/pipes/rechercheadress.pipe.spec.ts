import { RechercheadressPipe } from './rechercheadress.pipe';

describe('RechercheadressPipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheadressPipe();
    expect(pipe).toBeTruthy();
  });
});
